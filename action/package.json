{
  "name": "oidc-ssh-ca-action",
  "version": "0.1.0",
  "private": true,
  "description": "Get a short-lived SSH certificate from oidc-ssh-ca",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "ncc build src/main.ts -o dist/main --minify && ncc build src/cleanup.ts -o dist/cleanup --minify",
    "test": "tsc --noEmit && node --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0"
  }
}
