# セキュリティ

## 脆弱性の報告

**公開 issue を立てないでください。**

GitHub の [Security Advisories](https://github.com/yuniruyuni/oidc-ssh-ca/security/advisories/new)
から非公開で報告してください。

このツールは SSH の認可を担うため、脆弱性はそのまま利用者のサーバへの
不正アクセスに繋がります。修正を用意してから公開したいので、非公開の
経路を使ってください。

個人が趣味で維持しているプロジェクトです。応答に数日かかることがあります。

## 想定している脅威

このツールが防ぐことを意図しているもの:

- GitHub secrets に置かれた**長期の SSH 秘密鍵の漏洩**
- fork / PR / 別ワークフローからの、権限のないデプロイ実行
- リポジトリのリネームや、解放されたリポジトリ名の再取得による成りすまし
  (認可には数値 ID を使う)
- 発行された認証情報の濫用 (`force-command` で能力そのものを絞る)

## 防がないもの

以下は**このツールの範囲外**です。利用側で対処してください。

- **接続先ホストの検証。** クライアント側の資格情報を短命にしても、
  接続先を検証しなければ中間者にそのまま渡すことになります。
  `known_hosts` の管理は利用者の責任です (action の `known-hosts` 入力)。
- **ワークフローを実行できる人の制御。** リポジトリへ書き込める人は
  ワークフローを変更でき、ルールが許す範囲のデプロイを実行できます。
  ブランチ保護と GitHub Environment の承認で制御してください。
- **`force-command` の先で何が起きるか。** 実行されるスクリプトの安全性は
  利用者の責任です。引数の検証を怠れば、証明書で絞った意味が消えます。
- **CA 鍵が漏れた場合。** 全 principal 分の証明書を発行できます。
  鍵の保護は利用者の責任です。

## 既知の制限

現時点で対処していない事項です。**承知の上で利用してください。**

| 事項 | 内容 | 緩和策 |
|---|---|---|
| **レート制限が無い** | `/issue` は認証前に JWT の署名検証を行うため、大量のリクエストで CPU を消費させられます | インターネットへ直接晒さず、トンネルやリバースプロキシの背後に置く |
| **リプレイ対策が無い** | `jti` の単回使用を強制していません。トークンの有効期間内であれば、捕捉されたトークンで再度発行できます | トークンの捕捉には TLS の突破かランナーの侵害が必要で、その時点で証明書自体が取られています |
| **CA 鍵がプロセス内にある** | 別プロセスや HSM へ隔離していません | 署名はインターフェースとして切ってあり、後から隔離できる構造にはしてあります |
| **失効の仕組みが無い** | 発行済み証明書を個別に無効化できません | 有効期限の上限が 15 分。ルールの削除で以後の発行は止まります |
| **監査ログの保全をしない** | 標準出力へ構造化ログを出すだけです | journald 等で収集してください |

## 運用上の推奨

- `validity` は必要最小限にする (既定の想定は 5 分)
- `force_command` を必ず指定する。指定しないと証明書は任意コマンドを実行できます
- `extensions` は空のままにする。ポート転送とエージェント転送は許可リストにも入れていません
- `workflow_ref` と `job_workflow_ref` の**両方**を制約する
- CA 鍵は systemd の `LoadCredential` 等でサービスユーザから隔離する
